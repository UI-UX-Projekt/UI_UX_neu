<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#ffe71906"
                android:centerColor="#ffe71906"
                android:centerY="1.0"
                android:endColor="#ffe71906"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#ff00bd00"
                    android:centerColor="#ff00bd00"
                    android:centerY="1.0"
                    android:endColor="#ff00bd00"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>